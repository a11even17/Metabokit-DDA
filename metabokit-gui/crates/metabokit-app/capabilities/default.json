{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Dialogs for choosing inputs. No filesystem, shell or HTTP access is granted to the web layer — all file access and output-folder opening happens in Rust.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
